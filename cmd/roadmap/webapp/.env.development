# Loaded automatically by Vite when running in development mode (e.g.
# `npm run dev`). For local-only overrides create a `.env.local` or
# `.env.development.local` file (both are gitignored).

VITE_API_URL=http://localhost:8080
